# Banco de dados SQLite (arquivo único, portátil).
# Caminho relativo à raiz do projeto — o arquivo fica em ./dev.db (CLI e app usam o mesmo)
DATABASE_URL="file:./dev.db"

# Senha mestre do Painel Administrativo (/admin) — MVP, sem autenticação complexa.
# Troque este valor antes de usar em produção na secretaria.
ADMIN_PASSWORD="defina-uma-senha-aqui"
