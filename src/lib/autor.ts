/**
 * Quem fez o sistema (Tarefa 15).
 *
 * Um lugar só porque há dois leitores — o rodapé da tela de matrícula do
 * tablet e o pé da barra lateral do painel — e duas cópias do mesmo nome
 * divergem em silêncio no dia em que uma for corrigida. É o mesmo argumento
 * que tirou `semAcento` das actions na Tarefa 7.
 *
 * O crédito existe por autorização da coordenação (2026-09-16): foi a
 * contrapartida por um sistema feito por uma pessoa só. Quem for removê-lo
 * achando que é enfeite indevido numa interface institucional precisa saber
 * disso antes.
 *
 * Só nome e handle do GitHub, de propósito: é o identificador que não
 * envelhece com a pessoa. Telefone e e-mail ficam fora da interface — o
 * contato completo mora no README e no estudo de caso da wiki.
 */
export const AUTOR = {
  nome: "Viccenzo Boff",
  /** Como aparece no tablet, onde não há link: legível e digitável num celular. */
  githubTexto: "github.com/viccenzo-boff",
  githubUrl: "https://github.com/viccenzo-boff",
} as const;
